// eslint-disable-next-line no-unused-vars
import { ConfigOptions } from '../types'

const config: ConfigOptions = {
  api: {
    debug: true,
    domain: 'https://mock.ease.smhx.net/app/mock/17',
    key: ''
  }
}

export default config