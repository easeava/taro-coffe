// eslint-disable-next-line no-unused-vars
import { ConfigOptions } from '../types'

const config: ConfigOptions = {
  api: {
    debug: false,
    domain: 'https://api.ease.smhx.net',
    key: ''
  }
}

export default config